<Application
    x:Class="Hotel_Management_System.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Hotel_Management_System"
    StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style
            x:Key="DashboardPrimaryBtn"
            BasedOn="{StaticResource {x:Type Button}}"
            TargetType="Button">
            <Setter Property="Background" Value="#0029B7" />
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style
            x:Key="DashboardSecondaryBtn"
            BasedOn="{StaticResource {x:Type Button}}"
            TargetType="Button">
            <Setter Property="Background" Value="#00061B" />
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style x:Key="Heading" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
        </Style>
    </Application.Resources>
</Application>
